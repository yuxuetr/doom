# Minimal Doom Emacs TODOs

Progress: 5/5 complete

- DONE Document the minimal Doom fork design.
- DONE Enable only the requested language modules in `$DOOMDIR/init.el`.
- DONE Prune the Python module to built-in Python, LSP, and uv support.
- DONE Remove unrequested language module source directories.
- DONE Run `doom sync` and verify the resulting module set.
