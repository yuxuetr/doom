# Minimal Doom Emacs TODOs

Progress: 0/5 complete

- TODO Document the minimal Doom fork design.
- TODO Enable only the requested language modules in `$DOOMDIR/init.el`.
- TODO Prune the Python module to built-in Python, LSP, and uv support.
- TODO Remove unrequested language module source directories.
- TODO Run `doom sync` and verify the resulting module set.
